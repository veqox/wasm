# wasm

Minimal Rust + WASM example

## Build

```bash
# nightly toolchain
make
make build

# stable toolchain
make build-stable
```

## Run

```bash
cd web
miniserve . # or any other generic static http server
```
